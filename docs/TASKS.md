# Project tasks & backlog

**Purpose:** Running list of **product / repo changes** we intend to make (replacing ad-hoc notes). Not the same as [`agents/_operations/learnings.md`](../agents/_operations/learnings.md) (per-sync log) or [`proposed-rules.md`](../agents/_operations/proposed-rules.md) (instruction inbox for the `figma-sync` agent).

**How to use:** Append rows to the table below. Mark **Done** when merged; keep a short note or PR link if helpful.

| Status | Item |
|--------|------|
| Pending | **Dynamic Figma file URL** — today the default file URL lives in the root [`README.md`](../README.md); replace with config or env-driven source per client/deployment so the README is not hand-edited each time. |

_Add new rows as the backlog grows._
