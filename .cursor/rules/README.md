# Cursor rules (optional)

This folder is intentionally **empty** so instructions stay only under **`agents/`** (including **`agents/_skills/`**).

Add a **`.mdc` rule** here only when you need something that is **only for Cursor** (editor globs, MCP quirks, etc.) and should **not** live next to Claude agents or Agent Skills.
