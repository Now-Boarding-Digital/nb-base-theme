import type { Meta } from '@storybook/react-vite'
import { Dropdown } from './Dropdown'

const meta = {
  title: 'Internal/Dropdown (Hidden)',
  component: Dropdown,
  tags: ['autodocs'],
} satisfies Meta<typeof Dropdown>

export default meta
