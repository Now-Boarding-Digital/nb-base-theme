# No agent copies here

Agent definitions live only under **`agents/<name>/.agent.md`** at the repo root.

In Claude Code, **@‑mention** those paths (e.g. **`@agents/figma-sync/.agent.md`**). This repo does not duplicate agent folders under **`.claude/agents/<name>`** and does not ship **`.claude/commands/`** slash shortcuts.
