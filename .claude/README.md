# `.claude/`

This folder is **optional** for Claude Code. This repo does **not** define slash commands under **`.claude/commands/`** — use **`@agents/<name>/.agent.md`** (e.g. **`@agents/figma-sync/.agent.md`**) instead.

| Path | Purpose |
|------|---------|
| **`agents/README.md`** | Notes that canonical agents live only under **`../agents/<name>/`**. |

**Project entry:** [`README.md`](../README.md). **All instructions:** [`agents/`](../agents/) — including **`agents/_skills/`** and **`agents/__templates/`**.
