# Sample client — quirks

Optional prose for the default **`sample`** client: exceptions, contacts, naming oddities in Figma, or sync caveats.

Canonical machine-readable config: [`../../_config/clients/sample.json`](../../_config/clients/sample.json) (`fileKey`, `fileUrl`, `nodes.*`, etc.).
