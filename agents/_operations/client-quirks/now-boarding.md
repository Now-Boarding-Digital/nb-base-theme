# Now Boarding — client quirks

**Role:** Design partner; repo uses their **Now Boarding Design System Kit** as the sample/boilerplate Figma source.

## Figma pointers

Canonical machine-readable config: [`../../_config/clients/now-boarding.json`](../../_config/clients/now-boarding.json) (`fileKey`, `fileUrl`, `nodes.buttonsFrame`, etc.).

## Sync notes

- Treat as **reference implementation** for styleguidehero until a paying end-client profile exists.
- Add token/page node ids to the JSON `figma.nodes` map as you lock sections in the file.
